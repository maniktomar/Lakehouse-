name: lakehouse
version: "1.0.0"
config-version: 2

profile: lakehouse

model-paths:   ["models"]
test-paths:    ["tests"]
docs-paths:    ["docs"]
target-path:   "target"
clean-targets: ["target", "dbt_packages"]

models:
  lakehouse:
    staging:
      +materialized: table
      +schema: silver
    marts:
      +materialized: table
      +schema: gold
